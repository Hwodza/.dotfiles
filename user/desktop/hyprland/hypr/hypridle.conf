general {
  after_sleep_cmd=hyprctl dispatch dpms on
  before_sleep_cmd=loginctl lock-session
  ignore_dbus_inhibit=false
  ignore_systemd_inhibit=false
  inhibit_screensaver=true
  lock_cmd=pidof hyprlock || hyprctl dispatch exec hyprlock
  inhibit_sleep = 3
}

listener {
  on-lid= loginctl lock-session
}

listener {
  timeout = 300 # 5 min
  on-timeout=loginctl lock-session
}

listener {
    timeout = 330                                                     # 5.5min
    on-timeout = hyprctl dispatch dpms off                            # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r          # screen on when activity is detected after timeout has fired.
}

listener {
    timeout = 1800                                # 30min
    on-timeout = systemctl suspend                # suspend pc
    # on-resume = sleep 1 && hyprctl dispatch dpms on && brightnessctl -r          # screen on when activity is detected after timeout has fired.
}
